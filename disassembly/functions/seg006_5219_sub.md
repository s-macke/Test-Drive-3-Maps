; sub_13069 is a second consumer for SCENETTO.BIN entries and repeats the same 8-byte
; object-header parse as sub_12F6F.
;
; It reads bytes +0/+1 as the primary counts and advances to start+8 for the main vertex data.
; For the alternate path (DX > 0x200), it re-reads bytes +4/+5 and follows the word at +6,
; so the alternate shape starts at start+8+offset.
;
; This confirms the 8-byte header is not just a viewer guess: the original code has explicit
; consumers that treat SCENETTO.BIN entries as a header plus shape data block(s).
